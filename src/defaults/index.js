import components from './props.components';
import icons from './props.icons';
import localization from './props.localization';
import options from './props.options';

export const defaultProps = {
  actions: [],
  classes: {},
  columns: [],
  components: components,
  data: [],
  icons: icons,
  isLoading: false,
  title: 'Table Title',
  options: options,
  localization: localization,
  style: {}
};
